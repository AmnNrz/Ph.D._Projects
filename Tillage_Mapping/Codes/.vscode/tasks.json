{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "Convert Jupyter Notebook to Python script",
            "type": "shell",
            "command": "jupyter nbconvert --to script ${file}",
            "problemMatcher": [],
            "group": {
                "kind": "build",
                "isDefault": true
            }
        }
    ]
}